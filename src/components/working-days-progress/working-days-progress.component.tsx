import React from "react";
import { workProgress } from "../../utils/calendar.utils";

const WorkingDaysProgress = () => {
  return <pre>Working Days Progress: {workProgress}</pre>;
};

export default WorkingDaysProgress;
